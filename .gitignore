target/
var/
.claude/
.DS_Store
.cargo/config.toml
__pycache__/
*.pyc
*.lock
!Cargo.lock

# Local copies of HTML produced by `modal run analysis/modal_app.py` —
# generated artifacts, not source. Patterns are anchored to the repo
# root so a hand-authored doc anywhere else is unaffected.
/likes_concentration_*.html
/ratio_*.html
/attrition_*.html
/blocks_cleavage_*.html
/followers_distribution_*.html
/following_distribution_*.html
/organic_*.html
/organic_*.json
/organic_*_posts.parquet
/organic_*_posts.csv

# Locally downloaded snapshots. `modal volume get` puts a ~56 GiB
# snapshot.duckdb here; it must never reach a commit.
analysis/snapshot/
